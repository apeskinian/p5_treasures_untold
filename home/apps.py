from django.apps import AppConfig


class HomeConfig(AppConfig):
    """
    Configuration for the 'home' app.
    This app handles the homepage.
    """
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'home'
